# URL de l'API backend
# En local : IP de votre machine sur le réseau local (pas "localhost", le téléphone
# doit pouvoir joindre cette adresse sur le même Wi-Fi)
# En production : URL du backend déployé (ex. Render)
EXPO_PUBLIC_API_URL=http://192.168.1.64:3333
